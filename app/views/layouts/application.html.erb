<!DOCTYPE html>
<html>
  <head>
    <title>Refurbiked</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/flatpickr/dist/flatpickr.min.css">

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <% unless devise_controller? && (controller_name == "sessions")%>
      <nav class="navbar navbar-light bg-light shadow-sm">
        <div class="container">
          <a class="navbar-brand font-weight-bold" href="<%= root_path %>">Refurbiked</a>

          <ul class="navbar-nav d-flex flex-row">
            <li class="nav-item mx-3">
              <%= link_to "Home", root_path, class: "nav-link" %>
            </li>
            <li class="nav-item mx-3">
              <%= link_to "My Orders", orders_path, class: "nav-link" %>
            </li>
            <li class="nav-item mx-3">
              <%= link_to "My Bikes", my_bikes_path, class: "nav-link" %>
            </li>
          </ul>
        </div>
      </nav>
    <% end %>

    <%= yield %>
  </body>
</html>
