<h1 class="mb-4 text-center">Refurbiked - My Bikes</h1>

<div class="container">
  <div class="row">
    <% @bikes.each do |bike| %>
    <% orders = Order.where(bike_id: bike) %>
      <div class="col-md-4 mb-4">
        <div class="card shadow-sm">
        <img src="https://www.canyon.com/dw/image/v2/BCML_PRD/on/demandware.static/-/Sites-canyon-master/default/dw855963ab/images/full/full_2023_/2023/full_2023_3444_lux-trail-cf-9_P01_P5.png?sw=1064&sh=599&sm=fit&sfrm=png" class="card-img-top p-3 rounded" alt="Bike Image">
          <div class="card-body">
            <h2 class="h5 font-weight-bold"><%= bike.brand %> (<%= bike.year %>)</h2>
            <p class="text-muted"><strong>Type:</strong> <%= bike.bike_type %></p>
            <p class="text-muted"><strong>Color:</strong> <%= bike.color %></p>
            <p class="text-muted"><strong>Bike added:</strong> <%= bike.created_at %></p>
            <ul><strong>Bookings:</strong>
              <% orders.each do |order| %>
                <li><p>Booked by: <%= order.user.email %></p> <p>From: <%= order.rental_date %></p> <p>To: <%= order.rental_date_end %></p></li>
              <% end %>
            </ul>
            <%= link_to "Delete bike", bike_path(bike), class:"btn btn-outline-danger",
              data: {
                turbo_method: :delete,
                turbo_confirm: "Are you sure you want to delete this bike and all of the associated bookings?"
              }
            %>
          </div>
        </div>
      </div>
    <% end %>
    <div class="col-md-4 mb-4">
        <div class="card shadow-sm">
          <div class="card-body">
            <p><strong>Add a bike</strong></p>
            <%= simple_form_for @bike do |f| %>
              <%= f.input :photo, as: :file %>
              <%= f.input :bike_type %>
              <%= f.input :brand %>
              <%= f.input :year %>
              <%= f.input :color %>
              <%= f.submit "Add bike" %>
            <% end %>
          </div>
        </div>
      </div>

  </div>


</div>
